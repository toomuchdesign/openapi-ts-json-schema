openapi: 3.0.3
info:
  title: title
  description: description
  version: 1.0.0
components:
  schemas:
    Answer:
      type: string
      nullable: true
      enum:
        - yes
        - no

paths:
  /v1/path-1:
    parameters:
      - schema:
          const: from-path
        in: header
        name: headers-param-1
        required: true
        description: headers-param-1 description
      - schema:
          const: from-path
        in: header
        name: headers-param-2
        required: true
        description: headers-param-2 description
      - schema:
          const: from-path
        in: header
        name: headers-param-3
        required: true
        description: headers-param-3 description

    get:
      parameters:
        - schema:
            const: from-operation
          in: header
          name: headers-param-1
          required: true
          description: headers-param-1 description
        - schema:
            const: from-operation
          in: header
          name: headers-param-2
          required: true
          description: headers-param-2 description
        - schema:
            const: from-operation
          in: header
          name: headers-param-4
          required: true
          description: headers-param-4 description
        - schema:
            type: string
            enum:
              - foo
              - bar
          in: body
          required: true
          description: Body param description
        - schema:
            type: string
          in: path
          name: path-param-1
          required: true
          description: Path param description
        - schema:
            type: string
          in: query
          name: query-param-1
          required: true
          description: Query param 1 description
        - schema:
            $ref: '#/components/schemas/Answer'
          in: query
          name: deferenced-query-param
          required: true
          description: deferenced-query-param description
      responses:
        '200':
          content:
            application/json:
              schema:
                type: string

servers:
  - url: url
